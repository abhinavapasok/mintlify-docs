---
title: "Module page template"
sidebarTitle: "Writing template"
description: "Master template for product guide module pages. Copy this page to start a new module."
hidden: true
---

{/* AUTHORING NOTE: This is the master template for module pages. To document a new module:
   1. Copy this page's content to product-guide/<module-slug>.mdx
   2. Replace every [bracketed placeholder] and delete the authoring notes
   3. Replace TODO callouts with real content, and add screenshots to /images
   4. See product-guide/sales-invoices for a completed example
   Owner: Apsara */}

<Warning>
  This is the internal writing template, not user documentation. See [Sales invoices](/product-guide/sales-invoices) for a completed example.
</Warning>

## Overview

\[One or two paragraphs: what this module does, who uses it, and when. Mention how it connects to neighboring modules — e.g. "orders can be converted to invoices".\]

## Before you start

- **Permission needed:** \[module permission, e.g. _Invoices → View / Add / Edit_. Ask an administrator if an option is missing for you.\]
- **Settings to review:** \[module settings that change behavior, e.g. document numbering, default tax mode\]

## Key concepts

| Term | Meaning |
| --- | --- |
| \[Term\] | \[Plain-language definition\] |
| \[Status name\] | \[What the status means and what can happen next\] |

## Common workflows

### \[Task name, verb-first — e.g. "Create an invoice"\]

<Steps>
  <Step title="[First action]">
    \[What to click and what happens. One action per step.\]
  </Step>
  <Step title="[Second action]">
    \[Continue until the task is complete. End with how the user knows it worked.\]
  </Step>
</Steps>

{/* Add a screenshot after each workflow where it helps: ![alt text](/images/<module>-<workflow>.png) */}

### \[Next task name\]

\[Repeat the Steps pattern for each everyday task. Aim for the 3–6 most common tasks, not every possible action.\]

## Field reference

| Field | What it does |
| --- | --- |
| \[Field name\] | \[What it means, when it's required, and any validation rules\] |

## FAQs

<AccordionGroup>
  <Accordion title="[Question a user actually asks]">
    \[Short, direct answer. Link to the workflow above if the answer is "follow these steps".\]
  </Accordion>
</AccordionGroup>

## Related

- [\[Related module\]](/product-guide/[slug]) — \[why it's related\]